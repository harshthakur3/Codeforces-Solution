#include<bits/stdc++.h>
using namespace std;
int main(){
    string s;
    cin >> s;
    int n = s.size();
    int k[n];
    int s[n];
    int rubal;
    for(int i = 0; i < n ; i++){
        cin >> k[i];
    }
    for(int i = 0; i < n ; i++){
        cin >> s[i];
    }
    cin >> rubal;
}