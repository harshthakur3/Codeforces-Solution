#include<bits/stdc++.h>
using namespace std;
int main(){
    long long int n;
    cin >> n;
    while(n > 0){
        cout << "a";
        n--;
        if(n == 0) break;
        cout << "a";
        n--;
        if(n == 0) break;
        cout << "b";
        n--;
        if(n == 0) break;
        cout << "b";
        n--;
        if(n == 0) break;
    }
}